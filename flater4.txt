import 'package:egzam/xx.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
void main() {
  runApp(MyApp());
}

var tasks = [
  ' معلومات شخصيه ',
  ' معلومات شخصيه',
  ' معلومات شخصيه',
  ' معلومات شخصيه',
  ' معلومات شخصيه',


];

class MyApp extends StatelessWidget {
  @override


  Widget build(BuildContext context) {
    return Center(
      child: MaterialApp(
        home: Directionality(
            textDirection: TextDirection.rtl,
            child: Scaffold(
class MyApp extends StatelessWidget {
  @override


  Widget build(BuildContext context) {
    return Center(
      child: MaterialApp(
        home: Directionality(
            textDirection: TextDirection.rtl,
            child: Scaffold(


            appBar: AppBar(
              backgroundColor: Colors.white,

              title: const Text("اتخبار شامل ", style: TextStyle(color: Colors.black),),
              centerTitle: true,

            ),
                body: Padding(
                  padding: const EdgeInsets.fromLTRB(15, 0, 15, 0),
                  child: Column(


                    children: [
                  Stack(

                  children: [
                  CircleAvatar(radius: 100,child: Icon(Icons.person,size: 10,),),
                  Positioned(
                      bottom: 0,
                      left: 0,
                      child: CircleAvatar(child: Icon(Icons.access_time),))

                  ],
                ),
              Padding(
                padding: const EdgeInsets.all(5),
                child: TextFormField(
                  textDirection: TextDirection.rtl,
                    decoration: InputDecoration(
                      border: OutlineInputBorder(
                      borderRadius: BorderRadius.circular(15),
                    ),
                    hintText: "ادخل الاسم",

                    prefixIcon: Icon(Icons.person),
                  )
                  ),
               ),

                      Row(
                        children: [
                          Expanded(

                            




           